server:
  port: 8866
spring:
  application:
    name: config-server
  profiles:
      active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
logging:
  config: classpath:logback.xml






